{
  "name": "json-pretty-toml",
  "version": "1.0.0",
  "description": "Convert JSON to flat TOML with max 2-level keys",
  "main": "index.ts",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "json-pretty-toml": "tsx ./index.ts"
  },
  "scripts": {
    "test": "bun test",
    "start": "tsx index.ts",
    "format": "prettier --write ."
  },
  "keywords": [
    "json",
    "toml",
    "converter",
    "cli"
  ],
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.8.1",
    "tsx": "^4.21.0"
  }
}
